<UserControl x:Class="NI.Views.Controls.SoundPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="DeviceItemStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#20FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SliderStyle" TargetType="Slider">
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="0,8"/>
        </Style>
    </UserControl.Resources>

    <!-- Acrylic-style panel -->
    <Border Background="#E8202020" CornerRadius="14" 
            Width="320" MinHeight="200"
            RenderTransformOrigin="0.5,0">
        <Border.RenderTransform>
            <TranslateTransform x:Name="PanelTranslate" Y="0"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.3" Color="Black"/>
        </Border.Effect>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="VolumeIcon" Text="🔊" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Ses" FontSize="16" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Mute Toggle -->
                <Border x:Name="MuteButton" Grid.Column="1" Background="#30FFFFFF" 
                        CornerRadius="8" Padding="10,6" Cursor="Hand"
                        MouseLeftButtonDown="OnMuteClick">
                    <TextBlock x:Name="MuteIcon" Text="🔇" FontSize="14"/>
                </Border>
            </Grid>

            <!-- Volume Slider -->
            <Grid Grid.Row="1" Margin="0,4,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="🔈" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0" Opacity="0.7"/>
                
                <Slider x:Name="VolumeSlider" Grid.Column="1" 
                        Style="{StaticResource SliderStyle}"
                        ValueChanged="OnVolumeChanged"/>
                
                <TextBlock x:Name="VolumeText" Grid.Column="2" Text="75%" 
                           Foreground="White" FontSize="12" VerticalAlignment="Center" 
                           Margin="8,0,0,0" Width="35" TextAlignment="Right"/>
            </Grid>

            <!-- Output Device Label -->
            <TextBlock Grid.Row="2" Text="Çıkış Cihazı" Foreground="#AAA" FontSize="11" Margin="0,0,0,8"/>

            <!-- Device List -->
            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" MaxHeight="120">
                <StackPanel x:Name="DeviceList">
                    <!-- Devices populated in code-behind -->
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>

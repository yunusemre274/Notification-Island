<UserControl x:Class="NI.App.Views.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NI.App.ViewModels"
             Width="900" Height="70">

    <UserControl.DataContext>
        <vm:NotificationViewModel />
    </UserControl.DataContext>

    <UserControl.RenderTransform>
        <TranslateTransform x:Name="SlideTransform" Y="-80"/>
    </UserControl.RenderTransform>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="SlideTransform"
                                     Storyboard.TargetProperty="Y"
                                     From="-80" To="0" Duration="0:0:0.35">
                        <DoubleAnimation.EasingFunction>
                            <QuadraticEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:0.35"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Border CornerRadius="35" Width="900" Height="70">
        <Border.Background>
            <SolidColorBrush Color="#000000" Opacity="0.8"/>
        </Border.Background>
        <Grid Margin="24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Clock + status -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                <TextBlock Text="{Binding ClockText}" FontFamily="Segoe UI Semibold" FontSize="22" Foreground="White" VerticalAlignment="Center"/>
                <Ellipse Width="12" Height="12" Fill="#2EE36E" VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>

            <!-- Middle: App icon + text -->
            <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Width="40" Height="40" Source="{Binding CurrentNotification.Icon}" Margin="0,0,12,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentNotification.AppName}" FontWeight="SemiBold" Foreground="White" FontSize="15"/>
                    <TextBlock Text="{Binding CurrentNotification.Message}" Foreground="#CCFFFFFF" FontSize="13"/>
                </StackPanel>
            </StackPanel>

            <!-- Right icons -->
            <StackPanel Orientation="Horizontal" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Image Width="22" Height="22" Source="pack://siteoforigin:,,,/Assets/Icons/sound.png" Margin="0,0,12,0"/>
                <Image Width="22" Height="22" Source="pack://siteoforigin:,,,/Assets/Icons/wifi.png"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
